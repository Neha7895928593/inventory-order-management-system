# Frontend

React + Vite frontend for the inventory and order management dashboard.

## Commands

```bash
npm install
npm run dev
npm run build
npm run lint
```

## Environment

Create `.env` from `.env.example`:

```bash
VITE_API_BASE_URL=http://localhost:8000
```
